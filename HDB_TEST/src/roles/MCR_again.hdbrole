{
	"role":{
		"name": "MCR",
		"schema_privileges": [{
		   "privileges": ["SELECT METADATA",
		    			  "SELECT CDS METADATA",
		    			  "SELECT",
		    			  "INSERT INTO",
		    			  "EXECUTE",
		    			  "DELETE",
		    			  "UPDATE2",
		    			  "TRIGGER",
		    			  "CREATE TEMPORARY TABLE"
		   ]
		}]
		

      }
}
