{
	"role":{
		"name": "MCR",
		"schema_privileges": [{
		   "privileges": ["SELECT METADATA",
		    			  "SELECT CDS METADATA",
		    			  "SELECT",
		    			  "INSERT INTO",
		    			  "EXECUTE",
		    			  "DELETE",
		    			  "UPDATE",
		    			  "TRIGGER",
		    			  "CREATE TEMPORARY TABLE"
		   ]
		}]
		

      }
}
